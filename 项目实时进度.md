# 项目实时进度

更新时间：2026-05-15  
用途：记录本课程作业仓库的当前完成状态、GitHub 提交口径、可复现路径和推送后注意事项，方便人工与 Agent 快速接手。

---

## 1. 一句话概括

这是《因果推断与机器学习》课程作业项目，主题是：**用 Monte Carlo 模拟比较 `PSM`、`DID`、`DID-X`、`PSM-DID` 在不同识别环境下的表现，并结合真实 PSM-DID 论文完成文献评价。**

当前项目已经完成 GitHub 首次推送：研究报告、复现代码、主要图表、论文材料索引和 AI 工具使用记录均已提交到远端仓库。

---

## 2. 当前项目状态

### 已完成

- **任务 1：模拟设计与数据生成** 已完成
- **任务 2：PSM / DID / DID-X / PSM-DID 方法实现、结果汇总和诊断图表** 已完成
- **PSM 匹配专项审计** 已完成
- **DID 回归专项审计** 已完成
- **敏感性分析（Rosenbaum bounds + caliper 匹配）** 已完成
- **事件研究图（event study）** 已完成
- **任务 3：研究报告主体写作** 已完成
- **任务 4：真实 PSM-DID 论文评价** 已完成并入研究报告
- **AI 工具使用记录** 已更新到阶段 `8`
- **README 与 GitHub 交付清单** 已完成整理

### 当前提交状态

- 根目录 [README.md](README.md) 已更新为 GitHub 项目首页，包含环境安装、复现命令和交付材料位置。
- [reports/研究报告.md](reports/研究报告.md) 是正式 Markdown 报告。
- [reports/AI工具使用记录.md](reports/AI工具使用记录.md) 已记录关键 prompt、AI 主要输出、人工判断和采纳结果。
- [data/external/论文材料.md](data/external/论文材料.md) 已单独列出被评价论文的题名、作者、期刊、年份、DOI 和稳定链接。
- `.gitignore` 已调整为：提交轻量交付材料，忽略 `.venv/`、`.obsidian/`、大体量原始模拟数据、中间结果和运行日志。
- GitHub 远端仓库：`git@github.com:wty-bot/-Monte-Carlo-PSM-DID.git`
- GitHub 页面：https://github.com/wty-bot/-Monte-Carlo-PSM-DID
- 当前分支：`main`
- 首次提交：`15d2282 Initial coursework submission`



---

## 3. GitHub 提交材料对照表

| 课程要求 | 当前仓库位置 | 当前状态 |
|:---|:---|:---|
| 研究报告 Markdown | [reports/研究报告.md](reports/研究报告.md) | 已完成，含问题背景、模拟设计、结果展示、理论解释、文献评价和结论 |
| 可复现代码 | [scripts/](scripts/) 与 [src/](src/) | 已完成，脚本可从头生成数据、运行估计并输出主要图表 |
| 结果图表 | [outputs/figures/](outputs/figures/) 与 [outputs/checks/](outputs/checks/) | 已保留报告主图和自检图，适合 GitHub 查看 |
| 结果表格 | [outputs/tables/](outputs/tables/) 与 [data/processed/](data/processed/) | 已保留轻量汇总 CSV |
| 论文材料 | [data/external/论文材料.md](data/external/论文材料.md) | 已补齐题名、作者、期刊、年份、DOI 和稳定链接 |
| AI 工具使用记录 | [reports/AI工具使用记录.md](reports/AI工具使用记录.md) | 已更新到最终提交整理阶段 |

---

## 4. 复现路径

### 环境安装

```bash
python -m venv .venv
```

Windows PowerShell：

```powershell
.\.venv\Scripts\Activate.ps1
pip install -r requirements.txt
python scripts/check_environment.py
```

macOS / Linux：

```bash
source .venv/bin/activate
pip install -r requirements.txt
python scripts/check_environment.py
```

### 快速烟雾测试

```bash
python scripts/run_task1_generate.py --n-sims 10
python scripts/run_task2_estimate.py --smoke
python scripts/run_sensitivity.py
python scripts/run_event_study.py
```

### 完整复现报告结果

```bash
python scripts/run_task1_generate.py --n-sims 500
python scripts/run_task2_estimate.py --n-sims 500
python scripts/run_sensitivity.py
python scripts/run_event_study.py
```

说明：完整复现会生成 `data/raw/` 和 `data/interim/` 中的大体量文件。它们默认不提交到 GitHub，但可由上述命令重新生成。

---

## 5. 当前提交策略

### 建议提交到 GitHub 的内容

- 根目录说明：`README.md`、`PRD.md`、`课程作业.md`、`项目实时进度.md`
- 依赖与配置：`requirements.txt`、`.gitignore`、`.gitattributes`
- 核心代码：`src/`、`scripts/`
- 正式报告与记录：`reports/研究报告.md`、`reports/AI工具使用记录.md`、`reports/README.md`
- 过程文档：`docs/`
- 论文材料索引：`data/external/论文材料.md`
- 轻量汇总结果：`data/processed/task2_summary.csv`、`data/processed/task2_sensitivity_summary.csv`
- 报告主图和自检图：`outputs/figures/*.png`、`outputs/checks/*.png`、`outputs/checks/*.md`
- 报告主表：`outputs/tables/*.csv`

### 不建议提交到 GitHub 的内容

- `.venv/`：本地虚拟环境
- `.obsidian/`：本地编辑器配置
- `data/raw/` 下的大体量 parquet 模拟数据
- `data/interim/` 下的逐次模拟和事件研究中间 CSV
- `outputs/logs/` 下的运行日志
- `data/external/` 中可能存在的论文全文解析素材或图片资源

---

## 6. 核心研究结论

### 场景定义

- `A`：随机分配基准世界，无趋势异质性，`DID` 应表现最好。
- `B`：趋势差异由可观测变量 `X` 驱动，基础 `DID` 会偏，`DID-X` 和 `PSM-DID` 应修复。
- `C`：趋势差异由不可观测变量 `U` 驱动，`PSM-DID` 也会失败。
- `D`：保留 `B` 的识别结构，只把处理效应改为异质 `tau_i`，用于展示估计对象更接近 `ATT`。

### 主要数值结论

- `A`：四种方法基本都无偏；`DID` / `DID-X` 的 RMSE 最低。
- `B`：基础 `DID` 明显上偏，`bias ≈ +8.507`；`DID-X` 与 `PSM-DID` 基本修复成功。
- `C`：`DID`、`DID-X`、`PSM-DID` 都仍明显偏误；`PSM` 偏得最厉害，`bias ≈ +14.797`。
- `D`：`PSM-DID` 在识别上延续 `B` 的成功，同时数值上更贴近处理组局部 `ATT`。

### 方法论结论

- `PSM-DID` 不是无条件优于 `DID`。
- 当趋势差异完全由可观测 `X` 驱动时，匹配或参数化控制都可能修复偏误。
- 当不可观测 `U` 同时驱动处理分配和时间趋势时，仅平衡 `X` 不足以识别因果效应。
- 匹配会改变样本构成，因此在异质处理效应场景下，估计对象会更接近 `ATT` 而不是 `ATE`。

---

## 7. 当前最重要文件

如果只读 5 个文件，建议读：

1. [README.md](README.md)：GitHub 首页与复现说明
2. [reports/研究报告.md](reports/研究报告.md)：正式研究报告
3. [reports/AI工具使用记录.md](reports/AI工具使用记录.md)：AI 使用记录
4. [outputs/checks/task2_self_check_report.md](outputs/checks/task2_self_check_report.md)：任务二自检与结果摘要
5. [data/external/论文材料.md](data/external/论文材料.md)：论文引用信息

核心代码入口：

- [src/simulation/dgp.py](src/simulation/dgp.py)：四个场景的数据生成过程
- [src/estimation/methods.py](src/estimation/methods.py)：`PSM`、`DID`、`DID-X`、`PSM-DID` 与 event study 的核心实现
- [src/analysis/summarize.py](src/analysis/summarize.py)：结果汇总、识别诊断和图表生成
- [src/analysis/sensitivity.py](src/analysis/sensitivity.py)：Rosenbaum bounds 与 caliper 匹配
- [scripts/run_task1_generate.py](scripts/run_task1_generate.py)：任务一入口
- [scripts/run_task2_estimate.py](scripts/run_task2_estimate.py)：任务二入口
- [scripts/run_sensitivity.py](scripts/run_sensitivity.py)：敏感性分析入口
- [scripts/run_event_study.py](scripts/run_event_study.py)：事件研究入口

---

## 8. 推送后体检结果

截至 2026-05-15，本地检查结果如下：

- 当前分支：`main`
- 当前远端：`origin git@github.com:wty-bot/-Monte-Carlo-PSM-DID.git`
- 首次提交：`15d2282 Initial coursework submission`
- 本地分支已跟踪远端：`main...origin/main`
- 首次推送已成功完成：`main -> origin/main`
- `.venv/` 与 `.obsidian/` 已被忽略
- 大体量 `data/raw/` 与 `data/interim/` 已被忽略，只保留 `.gitkeep`
- 轻量交付结果已从 `.gitignore` 中放出，包括报告图表、汇总表和论文材料索引
- 使用项目虚拟环境运行 `scripts/check_environment.py` 已通过
- 使用系统 `python` 直接运行环境检查时缺少 `statsmodels` 和 `pyarrow`，因此复现时应按 README 先激活 `.venv` 或安装 `requirements.txt`
- Git 命令会提示无法访问 `C:\Users\yuntian/.config/git/ignore`，目前不影响仓库内忽略规则判断和远端推送；如需消除警告，可以检查本机全局 Git ignore 配置权限

---

## 9. 后续还需要做什么

从仓库内容和 Git 操作角度看，课程作业要求的材料已经提交到 GitHub。后续主要是人工提交课程平台前的确认事项：

1. **打开 GitHub 页面抽查显示效果**
   - 建议检查 README、研究报告、图表链接和中文路径是否正常显示。

2. **提交课程平台所需仓库地址**
   - 推荐提交 HTTPS 页面地址：https://github.com/wty-bot/-Monte-Carlo-PSM-DID
   - 如果老师要求克隆地址，也可提交 SSH 地址：`git@github.com:wty-bot/-Monte-Carlo-PSM-DID.git`

3. **建议人工最后通读研究报告**
   - 技术整理已完成，但提交前最好再通读 [reports/研究报告.md](reports/研究报告.md)，确认语气、字数、图表引用和文献评价口径。

---

## 10. 新 Agent 注意事项

- 仓库包含大量中文文件名，PowerShell 读取时请显式使用 UTF-8：`Get-Content -Encoding utf8`。
- 不要把项目退回到三场景版本；场景 `D` 是为了展示异质处理效应和 `ATT/ATE` 差异。
- 不需要重新设计 DGP，也不需要重写任务二方法口径。
- 如后续继续更新并推送，优先确认 `.gitignore` 下的提交清单，而不是直接把所有未忽略大文件强行加入。

---

## 11. 一句交接结论

**本项目的计算、诊断、研究报告、论文评价、AI 使用记录和 GitHub 交付说明均已完成，并已首次推送到 GitHub。当前不是继续做方法开发，而是进行 GitHub 页面抽查和课程平台提交。**
