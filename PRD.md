# 项目需求文档

## 一、项目目标

完成课程作业《用 Monte Carlo 模拟理解 PSM-DID》。
核心任务是：  
1. 自行设计数据生成过程；  
2. 在至少三个不同场景下比较 PSM、DID、PSM-DID 的估计表现；  
3. 基于模拟结果进行理论解释；  
4. 评价一篇使用 PSM-DID 的真实经济学论文。

Monte Carlo 的核心是通过重复随机抽样研究方法表现；DID 的核心是比较处理组和控制组前后变化之差；PSM 的核心是按倾向得分匹配处理组与控制组以提高可比性。

## 二、核心需求

### 任务1. 模拟设计


需要自行构造一个面板数据或重复截面数据的模拟环境，至少包含：

- 一个处理前协变量 \(X_i\)
- 两个时期：处理前、处理后
- 明确的处理分配机制 \(D_i\)
- 明确的结果生成过程
- 明确的真实处理效应 \(\tau\)
- 每个场景至少重复模拟 500 次 [web:32][web:42]

至少设计三个场景：

- **场景 A**：DID 表现较好的基准情形，处理组和控制组满足无条件平行趋势。
- **场景 B**：PSM-DID 可能优于 DID 的情形，趋势差异与处理前协变量 \(X_i\) 有关，匹配后条件平行趋势成立。
- **场景 C**：PSM-DID 也可能失败的情形，例如存在不可观测的时间变化混淆、共同支持不足，或错误匹配变量。
每个场景都需要说明：
- 处理组与控制组为何不同
- 差异是可观测还是不可观测
- 差异是否随时间变化
- 预期哪种方法更有效，哪种方法可能失效

### 任务2. 方法比较与保存

每个场景下至少比较三类方法：
- 单独 PSM
- 单独 DID
- PSM-DID
比较重点不是只看“谁更准”，而是说明在什么数据生成机制下，为什么某种方法更接近真实效应，为什么另一种方法会产生偏误。
需要保存每次模拟下各方法的估计结果，并汇总形成比较结果。至少展示：
- 平均估计值
- Bias
- RMSE
- 标准差
- 估计值分布图
- 匹配前后平衡情况
- 倾向得分共同支持图 [web:32][web:52][web:44]

### 任务3. 理论解释
需要基于模拟结果解释以下问题：

1. 处理选择只由可观测协变量决定时，PSM 为什么可能有效  
2. 存在时间不变未观测异质性时，DID 为什么可能有效  
3. 趋势差异依赖于 \(X_i\) 时，PSM-DID 为什么可能优于未匹配 DID  
4. 存在时间变化未观测混淆时，PSM-DID 为什么仍可能失败  
5. 匹配是否改变估计对象，估计更接近 ATE 还是 ATT  
6. 共同支持不足时，为什么偏误和方差都可能恶化 
理论解释必须和自己的模拟场景及结果对应，不能只做泛泛的教材复述。

### 任务4. 论文评价
需要选择一篇使用 PSM-DID 的真实论文，并至少评价以下内容：
- 研究问题和处理变量是什么
- 为什么使用 PSM-DID，而不是只用 PSM 或 DID
- 匹配变量是否都是处理前变量
- 是否报告共同支持和平衡性检验
- 平行趋势假设是否可信，作者如何检验
- 是否存在不可观测的时间变化混淆或同时发生政策
- 估计结果更接近 ATE 还是 ATT
- 结合本项目模拟经验，该论文在哪些条件下可信，在哪些条件下脆弱

## 三、交付物

### 1. 研究报告
提交 Markdown 格式研究报告，建议 3000–5000 字，至少包含：

- 问题背景
- 模拟设计
- 结果展示
- 理论解释
- 文献评价
- 结论

### 2. 可复现代码
代码应能够：

- 从头生成模拟数据
- 重复运行 Monte Carlo 模拟
- 实现 PSM、DID、PSM-DID
- 输出主要结果和图表

### 3. 结果图表
提交关键图表；若已嵌入报告可不单独提交。

### 4. 论文材料
提交论文题名、作者、期刊、年份、DOI 或稳定链接。

### 5. AI 使用记录
若使用 AI 工具辅助编程、写作、调试或文献梳理，需提交完整使用记录，包括：

- 关键 prompt
- 工具主要输出
- 修改与判断过程
- 最终采纳内容

## 四、验收标准

项目完成后，应满足以下要求：

- 至少三个场景，且每个场景逻辑清楚
- 每个场景至少 500 次模拟
- 每个场景都比较 PSM、DID、PSM-DID
- 至少输出一张汇总表，包含平均估计值、Bias、RMSE、标准差
- 至少输出若干关键图表
- 有逐场景理论解释，而不是只报告结果
- 完成一篇真实 PSM-DID 论文的结构化评价
- 保留完整 AI 使用记录并满足学术诚信要求

